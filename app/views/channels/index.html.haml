.drop-down-container
  %ul
    %li
      - @channels.each do |channel|
        .channel-information
          = link_to channel.name, channel_path(channel)
          %br
          = image_tag channel.videos.first.uri_url(:thumb)
