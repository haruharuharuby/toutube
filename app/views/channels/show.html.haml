%p#notice= notice

.container
  .row.title
    %h3 チャンネル #{@channel.name}
  .row.video-list
    %h4 このチャンネルに投稿された動画
    %ul.nav.navbar-nav
      - @channel.videos.each do |video|
        %li
          .thumbnail
            = link_to image_tag(video.uri_url(:thumb)), video
          %small
            .information
              .title
                = link_to video.title, video
              .author
                作者:
                = link_to video.channel.name, channel_path(video.channel)
              .view-count
                視聴回数
                #{video.view_count} 回 *
                #{time_ago_in_words video.created_at} 前
