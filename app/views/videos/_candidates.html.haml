%b オススメ
- videos.each do |video|
  %ul
    %li
      .thumbnail
        = image_tag video.uri_url(:thumb2) if video.uri?
      .information
        %b 'author'
        = video.channel.name
        %br
        %b 'interactions'
        = video.view_count
        %br
        = video.created_at
        %br
        %b
        = link_to video.title, video
