.title
  次の動画
.thumbnails
  - videos.each do |video|
    %ul.nav.nav-stacked
      %li
        .thumb.pull-left
          = link_to image_tag(video.uri_url(:thumb2), size: "108x72"), video
        %small
          .information
            .title
              = link_to video.title, video
            .author
              作者:
              = link_to video.channel.name, channel_path(video.channel)
            .view-count
              視聴回数
              #{video.view_count} 回 *
              #{time_ago_in_words video.created_at} 前
